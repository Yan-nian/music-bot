# Music Bot - 精简版依赖

# Telegram Bot
python-telegram-bot==21.9
telethon==1.34.0

# HTTP 请求
requests==2.31.0
httpx>=0.25.0

# Web 服务
flask==3.0.2
Werkzeug>=2.3.0

# 数据处理
mutagen>=1.47.0
eyed3>=0.9.7
tinytag>=1.10.0

# 图片处理（封面压缩）
Pillow>=10.0.0

# 加密支持
pycryptodome>=3.19.0

# 配置文件
tomli>=2.0.0

# 异步支持
aiofiles>=0.8.0

# 代理支持
PySocks==1.7.1

# Apple Music (可选，需要 gamdl)
# gamdl 默认注释：它是可选依赖，但写死会导致装不上时整个 pip install 失败。
# 如需 Apple Music 下载功能，手动安装：pip install gamdl>=2.4.0
# gamdl>=2.4.0

# 歌词支持
chardet>=5.0.0
